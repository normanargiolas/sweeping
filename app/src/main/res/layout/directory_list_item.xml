<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:attr/listPreferredItemHeight"
    android:paddingBottom="12dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="12dp">

    <ImageView
        android:id="@+id/directory_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:src="@drawable/folder" />

    <TextView
        android:id="@+id/directory_folder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        app:layout_constraintLeft_toRightOf="@+id/directory_icon"
        android:textColor="@color/app_name"
        tools:text="Today, April 03" />


    <TextView
        android:id="@+id/directory_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="@string/calcolo"
        android:textSize="@dimen/msg_text_third"

        app:layout_constraintLeft_toRightOf="@+id/directory_icon"
        app:layout_constraintTop_toBottomOf="@+id/directory_folder" />


</android.support.constraint.ConstraintLayout>